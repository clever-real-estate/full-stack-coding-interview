# Django Configuration
SECRET_KEY=your-secret-key-here-generate-with-django
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
SITE_NAME=Photo Gallery
DEFAULT_PAGE_SIZE=10

# JWT Token Configuration
ACCESS_TOKEN_MINUTES=15
REFRESH_TOKEN_DAYS=7
ROTATE_REFRESH_TOKENS=True
JWT_SIGNING_KEY=your-jwt-signing-key-here-generate-secure-key

# MySQL Database Configuration
DB_NAME=photo_gallery
DB_USER=photo_user
DB_PASSWORD=your-secure-database-password
DB_ROOT_PASSWORD=your-secure-root-password
DB_HOST=127.0.0.1
DB_PORT=3307

# CORS Settings for React frontend
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
FRONTEND_URL=http://localhost:5173

# Email Configuration (Use Ethereal for development, or configure your SMTP)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=your-ethereal-email@ethereal.email
SMTP_PASSWORD=your-ethereal-password
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@photogallery.com
ADMIN_EMAILS=admin@photogallery.com

# Redis Configuration (for caching and sessions)
REDIS_URL=redis://:your-redis-password@127.0.0.1:6379/1
REDIS_PASSWORD=your-redis-password

# Frontend Environment Variables for Development
# API Configuration
VITE_API_URL=http://127.0.0.1:8000/api

# App Configuration  
VITE_APP_NAME=Photo Gallery
VITE_APP_VERSION=0.0.1

# Development flags
VITE_DEBUG=true
VITE_ENABLE_CONSOLE_LOGS=true