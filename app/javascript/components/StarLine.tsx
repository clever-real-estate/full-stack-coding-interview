import React from 'react';

function StarLine({ className }: { className: string }) {
	return (
		<img src="/star-line.svg" alt="Star outline" className={className} />
	);
}

export default StarLine; 